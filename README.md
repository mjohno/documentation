# documentation

This repository contains the source which is built into HTML using mkdocs for
the github user site [mjohno.github.io](https://mjohno.github.io/).

## Developing

```
python3 -m venv venv
source venv/bin/activate
pip install -r requirements.txt
mkdocs build
```

